-- Create nexuspay database if it doesn't exist
-- Run this script using pgAdmin or your PostgreSQL client

-- Connect to PostgreSQL with user 'postgres' and password 'root'
-- Then execute:

CREATE DATABASE nexuspay;

-- You're all set! The Spring Boot application will create the tables automatically.
